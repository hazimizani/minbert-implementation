# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Datasets (download separately — see README)
data/

# Pre-trained model cache (downloaded by setup.sh / from_pretrained)
bert-base-uncased/
*.bin

# Trained model checkpoints — too large for git (~1.3 GB each)
*.pt

# Re-run training outputs (committed historical results live in results/)
outputs/

# OS / editors
.DS_Store
.idea/
.vscode/
